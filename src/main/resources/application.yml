spring:
  profiles:
    active: dev2 #加载不同配置文件
mybatis:
    type-aliases-package: com.im.pojo
    mapper-locations: classpath:mapper/*.xml

mapper:
    mappers: com.im.utils.MyMapper
    not-empty: false
    identity: MYSQL

pagehelper:
    helperDialect: mysql
    reasonable: true
    supportMethodsArguments: true
    params: count=countSql



